- name: Deploy to Tomcat
  hosts: web
  become: yes
 
  tasks:
     - name: Copy War File To Tomcat
       copy:
         src: "{{ lookup('env', 'WORKSPACE') }}/target/Wapp-1.0-SNAPSHOT.war"
         dest: "/opt/tomcat/webapps/Wapp-1.0-SNAPSHOT.war"
